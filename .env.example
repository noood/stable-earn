# 本地开发专用：生成 32 字节随机值并以 Base64 保存。
# 生产环境必须使用 Cloudflare Secret，不要把真实值提交到版本控制。
CREDENTIAL_ENCRYPTION_KEY=

# Cloudflare Access JWT 校验参数。它们不是密钥，但必须与 Zero Trust 中的应用一致。
TEAM_DOMAIN=https://your-team.cloudflareaccess.com
POLICY_AUD=
